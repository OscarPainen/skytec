# Skytec — dependencias de la app de escritorio
# Fase 1 (cimientos): solo UI. Cada fase agrega lo suyo.
PySide6>=6.6
qtawesome>=1.3
openpyxl>=3.1        # carga masiva de inventario (.xlsx) + plantilla
python-escpos>=3.0   # impresión térmica ESC/POS
pyusb>=1.2           # backend USB de python-escpos (opción "USB" en Ajustes,
                     # ya no es la conexión por defecto -- ver pywin32 abajo).
                     # En Windows hace falta además libusb-1.0.dll Y reemplazar
                     # el driver de la impresora por WinUSB (con Zadig o similar);
                     # sin eso pyusb no puede tomar el dispositivo aunque
                     # libusb esté instalado.
pywin32>=306; sys_platform == "win32"  # conexión "Windows" (por defecto):
                     # imprime a través del driver que Windows ya instaló
                     # para la impresora, sin tocar drivers USB a mano.
firebase-admin>=6.5  # sincronización con solicitudes de skytec-web
resend>=2.0           # recuperación de contraseña por correo
python-dotenv>=1.0    # carga SKYTEC_RESEND_* (y demás) desde .env al arrancar

# Fase 4 (empaquetado):
pyinstaller>=6.0

# Desarrollo (no se empaqueta con la app):
# pytest>=8.0
