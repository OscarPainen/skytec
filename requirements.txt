# Skytec — dependencias de la app de escritorio
# Fase 1 (cimientos): solo UI. Cada fase agrega lo suyo.
PySide6>=6.6
qtawesome>=1.3
openpyxl>=3.1        # carga masiva de inventario (.xlsx) + plantilla
python-escpos>=3.0   # impresión térmica ESC/POS

# Fase 6 (sincronización):       firebase-admin>=6.5
# Fase 7 (empaquetado):          pyinstaller>=6.0
